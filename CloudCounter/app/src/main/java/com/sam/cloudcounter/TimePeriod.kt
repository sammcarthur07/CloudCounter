package com.sam.cloudcounter // Make sure this matches your project's package name

/**
 * Enum representing different time periods for stats and graph calculations.
 */
enum class TimePeriod {
    MINUTELY,
    HOURLY,
    DAILY,
    WEEKLY,
    FORTNIGHTLY,
    MONTHLY,
    YEARLY,
    ALL_TIME,
}
    